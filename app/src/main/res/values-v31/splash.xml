<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Nur Hintergrund -->
        <item name="windowSplashScreenBackground">#0B1221</item>

        <!-- Kein Icon im nativen Teil -->
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>

        <!-- Kein Branding im nativen Teil, da Compose das schöner macht -->
        <item name="android:windowSplashScreenBrandingImage">@null</item>

        <item name="postSplashScreenTheme">@style/Theme.FahrstuhlKartenspiel</item>
    </style>
</resources>
